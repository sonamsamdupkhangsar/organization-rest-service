# Local Eureka profile: loads local database, server port, Eureka registry, and issuer endpoints.
SERVER_PORT: 8088

POSTGRES_USERNAME: test
POSTGRES_PASSWORD: test
POSTGRES_DBNAME: organization
POSTGRES_SERVICE: localhost:5432
DB_SSLMODE: disable

EUREKA_HOST: localhost:8761
EUREKA_USER: test
EUREKA_PASSWORD: test

ISSUER_ADDRESS: http://platform.openissuer.test:9001

ISSUER_URI: ${ISSUER_ADDRESS}
OAUTH_CLIENTS: oauth-client
JWT_SET_URI: ${ISSUER_ADDRESS}/oauth2/jwks

eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://${EUREKA_USER}:${EUREKA_PASSWORD}@${EUREKA_HOST}/eureka
